[metadata]
name = libactivitypub
version = attr: libactivitypub.VERSION

[options]
packages = find:

install_requires =
	pycryptodome
	pytz
	requests
	uuid6

[options.packages.find]
where = src
include = libactivitypub*

[options.package_data]
libactivitypub = *.pyi, py.typed
